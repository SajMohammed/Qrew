# Tolerate peer-dependency drift from bleeding-edge deps (e.g. TypeScript 7, just days old).
# If a real conflict appears at install, pin the offending package instead of hiding it.
auto-install-peers=true
strict-peer-dependencies=false
